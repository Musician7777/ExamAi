/**
 * Shared constants used across both client and server code.
 * Keep this file free of React / Next.js dependencies so it can be
 * imported from anywhere (utility modules, API routes, components, etc.).
 */

export const CONSENT_KEY = 'examai_cookie_consent';
